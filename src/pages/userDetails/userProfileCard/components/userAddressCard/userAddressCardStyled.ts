import styled from "styled-components";

export const UserAddressCardWrapper = styled.div`
  & h4 {
    background-color: rgba(128, 128, 128, 0.3);
    width: fit-content;
    height: fit-content;
    padding: 5px;
    border-radius: 6px;
    transform: translate(-5px, -5px);
    box-shadow: 0 0 1px 1px rgba(0, 0, 0, 0.3);
  }

  flex: 1 1 auto;
  box-shadow: 0 0 3px 1px rgba(0, 0, 0, 0.3);
  background: white;
  border-radius: 6px;
  padding: 5px;
  list-style: none;
`;
